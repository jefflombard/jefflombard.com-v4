import React from 'react';

const ProjectTagsFilter = () => (
  <div>test</div>
);

export default ProjectTagsFilter;

// (<div className="filter">filter by tag |
// {uniqueTags.map( tag => (<a className="tag" key={tag} href=
// "#">{tag}</a>))}</div>
